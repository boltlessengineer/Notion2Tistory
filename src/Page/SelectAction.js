const { clipboard } = require("electron");

const handleDownload = () => {};

const handleCopy = () => {};

const handleUpload = () => {};

module.exports = {
    handleDownload,
    handleCopy,
    handleUpload
};
